<UserControl x:Class="dockus.Dock.Controls.DockIcon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:dockus.Core.Converters">
    <UserControl.Resources>
        <converters:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
    </UserControl.Resources>

    <Grid ClipToBounds="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="62"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border
            x:Name="IconBlock"
            MouseRightButtonUp="IconBlock_MouseRightButtonUp"
            VerticalAlignment="Center" 
            Width="52" 
            Height="52" 
            Background="#AA292929" 
            CornerRadius="10" 
            Margin="3,0">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width" To="60" Duration="0:0:0.3" />
                                        <DoubleAnimation Storyboard.TargetProperty="Height" To="60" Duration="0:0:0.3" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width" To="52" Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetProperty="Height" To="52" Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Image 
                x:Name="IconImage"
                Width="36" 
                Height="36" 
                Source="{Binding Icon}" 
                Cursor="Hand"
                MouseLeftButtonDown="Icon_MouseLeftButtonDown"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center">
                <Image.Style>
                    <Style TargetType="Image">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Width" To="44" Duration="0:0:0.3"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Height" To="44" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="Width" To="36" Duration="0:0:0.3"/>
                                            <DoubleAnimation Storyboard.TargetProperty="Height" To="36" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
                <Image.ToolTip>
                    <ToolTip Content="{Binding Title}" />
                </Image.ToolTip>
            </Image>
        </Border>

        <Rectangle 
            Grid.Row="1"
            Width="12" 
            Height="3" 
            VerticalAlignment="Top" 
            HorizontalAlignment="Center" 
            Margin="2,0" 
            RadiusX="2" 
            RadiusY="2"
            Fill="{Binding IsRunning, Converter={StaticResource BoolToBrushConverter}}" />
    </Grid>
</UserControl>