<Window x:Class="dockus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:dockus"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        Title="Dockus" 
        Height="75"
        SizeToContent="Width"
        ResizeMode="NoResize"
        Top="10"
        WindowStartupLocation="Manual"
        SizeChanged="Window_SizeChanged">
    <Border Background="#C0000000" BorderBrush="#FF4A4A4A" BorderThickness="1" CornerRadius="26" Padding="15,0">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" VerticalContentAlignment="Center">
            <ItemsControl x:Name="DockItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Width="48" Height="48" Margin="5" Source="{Binding Icon}"
                               Cursor="Hand"
                               MouseLeftButtonDown="Icon_MouseLeftButtonDown">
                            <Image.ToolTip>
                                <ToolTip Content="{Binding Title}" />
                            </Image.ToolTip>
                        </Image>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Border>
</Window>